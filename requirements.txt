# SELECT * FROM Korea - 해커톤 공용 환경
# Python 3.12 (3.13/3.14 금지)
#
#   py -3.12 -m venv .venv
#   .venv\Scripts\activate
#   pip install --no-cache-dir -r requirements.txt

# ---- 데이터 처리 ----
pandas==2.2.3
numpy==1.26.4
openpyxl==3.1.5
chardet==5.2.0

# ---- 분석 / 시각화 ----
scikit-learn==1.5.2
matplotlib==3.9.2

# ---- LLM / 임베딩 ----
openai==1.54.4
python-dotenv==1.0.1
tiktoken==0.8.0

# ---- 검색 ----
rank-bm25==0.2.2
kiwipiepy==0.18.1

# ---- 문서 처리 ----
pdfplumber==0.11.4

# ---- UI ----
streamlit==1.39.0

# ---- 유틸 ----
tqdm==4.66.5

# ============================================================
# 제거한 것과 근거
# ============================================================
# google-generativeai
#   : Gemini는 "OpenAI 결제 실패 시 대안"으로 넣었는데,
#     google-api-python-client(15.6MB) + grpcio(5MB) + protobuf +
#     google-auth 등 의존성 트리가 100MB를 넘습니다.
#     실제로 필요해지면 그때 pip install 하면 30초입니다.
# plotly
#   : matplotlib 과 역할 중복. streamlit 이 altair 를 이미 포함합니다.
#     19MB 휠 -> 설치 시 50MB+.
# xlrd
#   : 구형 .xls 대비용. 공공데이터는 대부분 CSV/XLSX 입니다.
#     .xls 파일을 실제로 만나면 그때 설치하세요.
# torch, sentence-transformers
#   : 임베딩을 OpenAI API로 결정. 불필요.
# chromadb, langchain, faiss
#   : 앞선 결정 참조.