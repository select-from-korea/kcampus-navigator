# SELECT * FROM Korea - 해커톤 공용 환경
# Python 3.11 또는 3.12 (3.13은 일부 패키지 미지원, 피할 것)
#
# 설치:
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)
#   source .venv/bin/activate     (macOS)
#   pip install -r requirements.txt

# ---- 데이터 처리 ----
pandas==2.2.3
numpy==1.26.4
openpyxl==3.1.5
xlrd==2.0.1
chardet==5.2.0

# ---- 분석 / 시각화 ----
scikit-learn==1.5.2
matplotlib==3.9.2
plotly==5.24.1

# ---- LLM / 임베딩 ----
openai==1.54.4
google-generativeai==0.8.3
python-dotenv==1.0.1
tiktoken==0.8.0

# ---- 검색 ----
rank-bm25==0.2.2
kiwipiepy==0.18.1

# ---- 문서 처리 ----
pdfplumber==0.11.4

# ---- UI ----
streamlit==1.39.0

# ---- 유틸 ----
tqdm==4.66.5

# ============================================================
# 의도적으로 제외 - 추가하지 마세요
# ============================================================
# torch, sentence-transformers
#   : 임베딩을 OpenAI API로 결정했으므로 불필요.
#     Windows 설치에 30분+ 소요되고 8GB RAM에서 부담이 큽니다.
# chromadb
#   : fastapi/uvicorn/kubernetes 등 40개+ 의존성을 끌고 옵니다.
#     우리 규모에서는 numpy 코사인 유사도가 더 빠릅니다.
# langchain
#   : 12시간 해커톤에서 추상화 레이어의 디버깅 비용이 이득보다 큽니다.